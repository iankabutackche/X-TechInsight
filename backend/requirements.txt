# X-TechInsight 第一阶段：核心 RAG 引擎依赖
# PDF 解析
pypdf>=4.0.0

# LangChain 生态（文档加载、切分、LLM 调用）
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

# 向量数据库
chromadb>=0.5.0

# 环境变量（API Key 放 .env 里）
python-dotenv>=1.0.0
